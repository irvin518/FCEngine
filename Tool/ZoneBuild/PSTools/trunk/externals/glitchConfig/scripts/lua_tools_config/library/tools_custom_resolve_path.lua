-- This function allow the user to customize a path when it is resolved,
-- Called by the tools, on importing a file and exporting a path.

function customResolvePathOnImport(path)
	local customPath = path
	return customPath
end

function customResolvePathOnExport(path)
	local customPath = path
	return customPath
end

